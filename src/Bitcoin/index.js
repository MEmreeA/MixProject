import * as React from 'react';
import {View, Text} from 'react-native';

function BtcScreen() {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: 'blue',
      }}>
      <Text style={{fontSize: 50, color: 'yellow'}}>DENEME PART</Text>
    </View>
  );
}

export default BtcScreen;
